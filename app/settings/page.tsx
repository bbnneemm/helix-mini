"use client";
import { useEffect, useState } from "react";
export default function SettingsPage() { const [baseUrl,setBaseUrl]=useState("https://api.openai.com/v1"),[apiKey,setApiKey]=useState(""),[model,setModel]=useState("gpt-4o-mini"),[configured,setConfigured]=useState(false),[status,setStatus]=useState(""); useEffect(()=>{fetch("/api/model-config").then(r=>r.json()).then(c=>{setBaseUrl(c.baseUrl);setModel(c.model);setConfigured(c.configured);setApiKey(c.apiKeyMasked||"")})},[]); const save=async()=>{setStatus("保存中…");const r=await fetch("/api/model-config",{method:"PUT",headers:{"Content-Type":"application/json"},body:JSON.stringify({baseUrl,apiKey:apiKey.includes("••••")?null:apiKey,model})});const data=await r.json();if(!r.ok){setStatus(data.error||"保存失败");return}setConfigured(data.configured);setApiKey(data.apiKeyMasked||"");setStatus("已保存");}; const clear=async()=>{await fetch("/api/model-config",{method:"DELETE"});setConfigured(false);setApiKey("");setStatus("已清除，Agent 将使用环境变量或 Mock 模式")}; return <main className="settings-page"><div className="settings-card"><a href="/">← 返回对话</a><h1>模型配置</h1><p className="settings-tip">配置会保存在项目内的 .data/model-config.json。API Key 仅用于本地调用，不会显示在日志中。</p><label>Base URL<input value={baseUrl} onChange={e=>setBaseUrl(e.target.value)} placeholder="https://api.openai.com/v1"/></label><label>API Key<input type="password" value={apiKey} onChange={e=>setApiKey(e.target.value)} placeholder={configured?"已配置，输入新 Key 可替换":"sk-…"}/></label><label>模型名称<input value={model} onChange={e=>setModel(e.target.value)} placeholder="gpt-4o-mini"/></label><div className="settings-actions"><button onClick={save}>保存配置</button><button onClick={clear}>清除配置</button>{status&&<span>{status}</span>}</div></div></main> }
